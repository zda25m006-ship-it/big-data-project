# Dashboards

Grafana dashboard JSON exports are stored in `../grafana/provisioning/dashboards/`.

To export your current Grafana dashboard:
1. Open Grafana at http://localhost:3001
2. Open a dashboard → Share → Export → Save to file
3. Place the exported JSON in `grafana/provisioning/dashboards/`
